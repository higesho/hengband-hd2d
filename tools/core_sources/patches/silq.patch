diff --git a/src/cmd3.c b/src/cmd3.c
index 6ef285f..fd62df9 100644
--- a/src/cmd3.c
+++ b/src/cmd3.c
@@ -1963,6 +1963,17 @@ void do_cmd_look(void)
 /*
  * Allow the player to examine other sectors on the map
  */
+/*
+ * TRUE while do_cmd_locate() is running (the HD2D adapter reads this).
+ *
+ * The 3D screen builds its view window around the player, so a shifted panel
+ * is invisible to it: pressing a direction changed p_ptr->wy/wx and nothing
+ * moved on screen.  Guessing from "is the player outside the panel?" is not
+ * enough -- on a wide floor the player often stays inside after a shift.
+ * The command itself is the only thing that knows, so it says so.
+ */
+bool sq_locate_shifted = FALSE;
+
 void do_cmd_locate(void)
 {
     int dir, y1, x1, y2, x2;
@@ -1971,6 +1982,8 @@ void do_cmd_locate(void)
     y2 = y1 = p_ptr->wy;
     x2 = x1 = p_ptr->wx;
 
+    sq_locate_shifted = TRUE;
+
     /* Show panels until done */
     while (TRUE)
     {
@@ -2032,6 +2045,8 @@ void do_cmd_locate(void)
         }
     }
 
+    sq_locate_shifted = FALSE;
+
     /* Verify panel */
     p_ptr->update |= (PU_PANEL);
 
diff --git a/src/files.c b/src/files.c
index 9874b4b..5e86efd 100644
--- a/src/files.c
+++ b/src/files.c
@@ -9,6 +9,7 @@
  */
 
 #include "angband.h"
+#include "sq_lang_c.h" //SQ: japanese layer (docs/SILQ_JA_DESIGN.md 4)
 
 // These are copied from birth.c and needed for displaying the character sheet
 #define INSTRUCT_ROW 21
@@ -1389,15 +1390,16 @@ void display_player_xtra_info(int mode)
         strnfmt(buf, sizeof(buf), "%s",
             b_name + (&b_info[ability_index(S_SNG, p_ptr->song1)])->name);
         Term_putstr(col, 11 + mod, -1, TERM_WHITE, "Song");
-        Term_putstr(
-            col + 5, 11 + mod, -1, TERM_L_BLUE, format("%11s", buf + 8));
+        //SQ: site #12 - "Song of " is a prefix only in english (see xtra1.c prt_song)
+        Term_putstr(col + 5, 11 + mod, -1, TERM_L_BLUE, //SQ:
+            format("%11s", buf + (sq_lang_enabled() ? 0 : 8))); //SQ:
     }
     if (p_ptr->song2 != SNG_NOTHING)
     {
         strnfmt(buf, sizeof(buf), "%s",
             b_name + (&b_info[ability_index(S_SNG, p_ptr->song2)])->name);
-        Term_putstr(
-            col + 5, 12 + mod, -1, TERM_L_BLUE, format("%11s", buf + 8));
+        Term_putstr(col + 5, 12 + mod, -1, TERM_L_BLUE, //SQ: site #12
+            format("%11s", buf + (sq_lang_enabled() ? 0 : 8))); //SQ:
     }
 
     /* Right */
@@ -1616,19 +1618,24 @@ static void display_player_flag_info(void)
  */
 static void display_player_misc_info(void)
 {
+    //SQ: site #14 - the value column moves from 8 to 6 in japanese.
+    //SQ: the labels are only 4 bytes there (`名前` `種族` `家`), and the House
+    //SQ: field has to hold `フィンゴルフィン` (16 bytes) before `Weight` at col 22.
+    const int sq_col = sq_lang_enabled() ? 6 : 8; //SQ:
+
     /* Name */
     put_str("Name", 2, 1);
-    c_put_str(TERM_L_BLUE, op_ptr->full_name, 2, 8);
+    c_put_str(TERM_L_BLUE, op_ptr->full_name, 2, sq_col); //SQ:
 
     /* Race */
     put_str("Race", 3, 1);
-    c_put_str(TERM_L_BLUE, p_name + rp_ptr->name, 3, 8);
+    c_put_str(TERM_L_BLUE, p_name + rp_ptr->name, 3, sq_col); //SQ:
 
     if (p_ptr->phouse)
     {
         /* Title */
         put_str("House", 4, 1);
-        c_put_str(TERM_L_BLUE, c_name + hp_ptr->short_name, 4, 8);
+        c_put_str(TERM_L_BLUE, c_name + hp_ptr->short_name, 4, sq_col); //SQ:
     }
 }
 
@@ -2048,14 +2055,15 @@ bool show_buffer(cptr main_buffer, cptr what, int line)
         else
         {
             /* Wait for it */
+            /*
+             * **桁を決め打ちにした白の重ね書きをやめた**（2026-08-25）。
+             * 8 / 21 / 42 / 49 桁は英語の綴りに合わせたもので、訳した行では
+             * まるで違う所へ落ちる（実機の画で語の途中に白い塊が出ていた）。
+             * 1 色で出せば、どの言語でも崩れない。
+             */
             Term_putstr(1, hgt - 2, -1, TERM_SLATE,
-                "(press ESC to exit, Space for next page, Arrows/Keypad to "
-                "scroll)");
-            Term_putstr(8, hgt - 2, -1, TERM_L_WHITE, "ESC");
-            Term_putstr(21, hgt - 2, -1, TERM_L_WHITE, "Space");
-            Term_putstr(42, hgt - 2, -1, TERM_L_WHITE, "Arrows");
-            Term_putstr(49, hgt - 2, -1, TERM_L_WHITE, "Keypad");
-            Term_putstr(67, hgt - 2, -1, TERM_L_WHITE, "");
+                "(ESC to exit, Right or Space for the next page, Left for the "
+                "previous, Up/Down to scroll)");
         }
 
         /* Get a keypress */
@@ -2413,14 +2421,15 @@ bool show_file(cptr name, cptr what, int line)
         else
         {
             /* Wait for it */
+            /*
+             * **桁を決め打ちにした白の重ね書きをやめた**（2026-08-25）。
+             * 8 / 21 / 42 / 49 桁は英語の綴りに合わせたもので、訳した行では
+             * まるで違う所へ落ちる（実機の画で語の途中に白い塊が出ていた）。
+             * 1 色で出せば、どの言語でも崩れない。
+             */
             Term_putstr(1, hgt - 2, -1, TERM_SLATE,
-                "(press ESC to exit, Space for next page, Arrows/Keypad to "
-                "scroll)");
-            Term_putstr(8, hgt - 2, -1, TERM_L_WHITE, "ESC");
-            Term_putstr(21, hgt - 2, -1, TERM_L_WHITE, "Space");
-            Term_putstr(42, hgt - 2, -1, TERM_L_WHITE, "Arrows");
-            Term_putstr(49, hgt - 2, -1, TERM_L_WHITE, "Keypad");
-            Term_putstr(67, hgt - 2, -1, TERM_L_WHITE, "");
+                "(ESC to exit, Right or Space for the next page, Left for the "
+                "previous, Up/Down to scroll)");
         }
 
         /* Get a keypress */
@@ -2525,6 +2534,24 @@ bool show_file(cptr name, cptr what, int line)
             line = line + (hgt - 5);
         }
 
+        /*
+         * 左右で頁を送る／戻す（利用者の指示 2026-08-25
+         * 「マニュアルは左右でページ送り戻りとして」）。
+         *
+         * 上流は 4 と 6 を使っていない（8/2 が 1 行、9/3 が 1 頁、7/1 が端）。
+         * パッドの十字は 4/6 で届くので、ここへ足すだけで手が届く。
+         * 上下（8/2）は 1 行送りのまま残す。読み返すときに要る。
+         */
+        if (ch == '6')
+        {
+            line = line + (hgt - 5);
+        }
+
+        if (ch == '4')
+        {
+            line = line - (hgt - 5);
+        }
+
         /* Advance to the bottom */
         if (ch == '1')
         {
@@ -3011,8 +3038,11 @@ void do_cmd_help(void)
 
         /* Prompt */
         c_put_str(TERM_L_WHITE, "(press any key)", 23, 53);
+        sq_help_manual_hint(); //SQ: japanese manual (docs/SILQ_JA_DESIGN.md 8.6)
         ch = inkey();
 
+        if (sq_help_manual_open(ch)) continue; //SQ: japanese manual
+
         /* Most keys take you a page forwards, 'up' or '-' or '8' take you back
          */
         if (ch != EOF)
@@ -3796,28 +3826,30 @@ extern void display_single_score(
     /* Possibly ammend the first line */
     if (the_score->morgoth_slain[0] == 't')
     {
-        my_strcat(out_val, ", who defeated Morgoth in his dark halls",
+        //SQ: site #13 - these are glued on after the format, so neither #3 nor
+        //SQ: #1/#2 can reach them (the line starts with the digits of the depth)
+        my_strcat(out_val, sq_tr(", who defeated Morgoth in his dark halls"), //SQ:
             sizeof(out_val));
     }
     else
     {
         if (the_score->silmarils[0] == '1')
         {
-            my_strcat(out_val, ", who freed a Silmaril", sizeof(out_val));
+            my_strcat(out_val, sq_tr(", who freed a Silmaril"), sizeof(out_val)); //SQ: site #13
         }
         if (the_score->silmarils[0] == '2')
         {
-            my_strcat(out_val, ", who freed two Silmarils", sizeof(out_val));
+            my_strcat(out_val, sq_tr(", who freed two Silmarils"), sizeof(out_val)); //SQ: site #13
         }
         if (the_score->silmarils[0] == '3')
         {
-            my_strcat(
-                out_val, ", who freed all three Silmarils", sizeof(out_val));
+            my_strcat(out_val, sq_tr(", who freed all three Silmarils"), //SQ: site #13
+                sizeof(out_val)); //SQ:
         }
         if (the_score->silmarils[0] > '3')
         {
-            my_strcat(out_val, ", who freed suspiciously many Silmarils",
-                sizeof(out_val));
+            my_strcat(out_val, sq_tr(", who freed suspiciously many Silmarils"), //SQ: site #13
+                sizeof(out_val)); //SQ:
         }
     }
 
@@ -3833,12 +3865,12 @@ extern void display_single_score(
         if ((the_score->morgoth_slain[0] == 't')
             || (the_score->silmarils[0] > '0'))
         {
-            my_strcat(out_val, " and brought back the light of Valinor",
-                sizeof(out_val));
+            my_strcat(out_val, sq_tr(" and brought back the light of Valinor"), //SQ: site #13
+                sizeof(out_val)); //SQ:
         }
         else
         {
-            my_strcat(out_val, " empty-handed", sizeof(out_val));
+            my_strcat(out_val, sq_tr(" empty-handed"), sizeof(out_val)); //SQ: site #13
         }
     }
 
@@ -3858,7 +3890,7 @@ extern void display_single_score(
         /* Mark those with a silmaril */
         if (the_score->silmarils[0] > '0')
         {
-            my_strcat(out_val, " during a daring escape", sizeof(out_val));
+            my_strcat(out_val, sq_tr(" during a daring escape"), sizeof(out_val)); //SQ: site #13
         }
     }
 
diff --git a/src/h-config.h b/src/h-config.h
index 1990f26..0cb5ff2 100644
--- a/src/h-config.h
+++ b/src/h-config.h
@@ -33,7 +33,15 @@
  * Basically, SET_UID should *only* be set for "Unix" machines.
  */
 
-#if !defined(WINDOWS)
+/*
+ * //SQ: Android also gets the WINDOWS-like configuration (2026-08-21).
+ * With SET_UID on, safe_setuid_grab() in util.c calls setregid(), and the
+ * Android seccomp filter kills the whole process with SIGSYS (measured on
+ * an emulator: "Fatal signal 31 (SIGSYS) ... syscall 114"). Android is a
+ * single-user sandbox per app, so none of what SET_UID buys us applies.
+ * See docs/ANDROID_HD2D_PORT.md 2b.4.
+ */
+#if !defined(WINDOWS) && !defined(__ANDROID__)
 #define SET_UID
 #endif
 
diff --git a/src/load.c b/src/load.c
index 5b7a9a6..218258f 100644
--- a/src/load.c
+++ b/src/load.c
@@ -9,6 +9,7 @@
  */
 
 #include "angband.h"
+#include "sq_lang_c.h" //SQ: japanese layer (docs/SILQ_JA_DESIGN.md 4)
 
 #include "init.h"
 
@@ -1579,6 +1580,10 @@ static errr rd_savefile_new_aux(void)
     /* Operating system info */
     rd_u32b(&sf_xtra);
 
+    //SQ: site #22 (A3) - the encoding mark rides in sf_xtra; note it before any
+    //SQ: rd_string() runs, or the nine human-text fields are read blind
+    sq_save_enc_note((unsigned long)sf_xtra); //SQ:
+
     /* Time of savefile creation */
     rd_u32b(&sf_when);
 
diff --git a/src/monster1.c b/src/monster1.c
index f7ad22c..b6bc8fa 100644
--- a/src/monster1.c
+++ b/src/monster1.c
@@ -9,6 +9,7 @@
  */
 
 #include "angband.h"
+#include "sq_lang_c.h" //SQ: japanese layer (docs/SILQ_JA_DESIGN.md 4)
 
 /*
  * Pronoun arrays, by gender.
@@ -350,7 +351,10 @@ static void describe_monster_drop(int r_idx, const monster_lore* l_ptr)
         if (l_ptr->drop_item)
         {
             /* Handle singular "an" */
-            if (sin)
+            //SQ: site #15 - the english article and plural hacks emit a bare
+            //SQ: "n" / "s"; the catalog cannot map a key to nothing, and both
+            //SQ: letters are keys of their own elsewhere (the sheet footer)
+            if (sin && !sq_lang_enabled()) //SQ:
                 text_out("n");
             sin = FALSE;
 
@@ -363,7 +367,7 @@ static void describe_monster_drop(int r_idx, const monster_lore* l_ptr)
                 text_out(" chest");
             else
                 text_out(" object");
-            if (n != 1)
+            if ((n != 1) && !sq_lang_enabled()) //SQ: site #15
                 text_out("s");
         }
 
diff --git a/src/monster2.c b/src/monster2.c
index a4c22f7..f9d8df7 100644
--- a/src/monster2.c
+++ b/src/monster2.c
@@ -9,6 +9,7 @@
  */
 
 #include "angband.h"
+#include "sq_lang_c.h" //SQ: japanese layer (docs/SILQ_JA_DESIGN.md 4)
 
 /*
  * Return another race for a monster to polymorph into.  -LM-
@@ -921,6 +922,10 @@ void monster_desc(char* desc, size_t max, const monster_type* m_ptr, int mode)
     cptr name;
     bool seen, pron;
 
+    //SQ: site #7 - articles and plurals go away in japanese; the adapter builds it
+    if (sq_monster_desc_ja(desc, max, m_ptr, mode)) //SQ:
+        return; //SQ:
+
     if (p_ptr->image)
     {
         r_ptr = &r_info[m_ptr->image_r_idx];
diff --git a/src/object1.c b/src/object1.c
index 824760d..5db5b09 100644
--- a/src/object1.c
+++ b/src/object1.c
@@ -9,6 +9,7 @@
  */
 
 #include "angband.h"
+#include "sq_lang_c.h" //SQ: japanese layer (docs/SILQ_JA_DESIGN.md 4)
 
 static void flavor_assign_fixed(void)
 {
@@ -594,6 +595,10 @@ void object_desc(
 
     object_kind* k_ptr = &k_info[o_ptr->k_idx];
 
+    //SQ: site #6 - the japanese word order differs; the adapter builds the whole name
+    if (sq_object_desc_ja(buf, max, o_ptr, pref, mode)) //SQ:
+        return; //SQ:
+
     /* Extract some flags */
     object_flags(o_ptr, &f1, &f2, &f3);
 
diff --git a/src/save.c b/src/save.c
index 4291d03..e7ba5a6 100644
--- a/src/save.c
+++ b/src/save.c
@@ -9,6 +9,7 @@
  */
 
 #include "angband.h"
+#include "sq_lang_c.h" //SQ: japanese layer (docs/SILQ_JA_DESIGN.md 4)
 
 static const char* races[] = {
     "Noldor",
@@ -1221,8 +1222,9 @@ static bool wr_savefile(void)
     /* Guess at the current time */
     now = time((time_t*)0);
 
-    /* Note the operating system */
-    sf_xtra = 0L;
+    //SQ: site #21 (A3) - sf_xtra is the spare u32 in the header; upstream writes 0
+    //SQ: and throws it away on load, so it is where the encoding mark goes
+    sf_xtra = sq_save_enc_mark(); //SQ:
 
     /* Note when the file was saved */
     sf_when = now;
diff --git a/src/util.c b/src/util.c
index 967898e..8ce41ce 100644
--- a/src/util.c
+++ b/src/util.c
@@ -9,6 +9,7 @@
  */
 
 #include "angband.h"
+#include "sq_lang_c.h" //SQ: japanese layer (docs/SILQ_JA_DESIGN.md 4)
 
 #ifdef SET_UID
 
@@ -493,7 +494,7 @@ errr my_fgets(FILE* fff, char* buf, size_t n)
         }
 
         /* Ignore non-printables */
-        else if (isprint(c))
+        else if (sq_is_text_byte(c)) //SQ: site #8 - lib-ja/edit is CP932; isprint() eats it
         {
             /* Store character in the buffer */
             *s++ = c;
@@ -1689,6 +1690,7 @@ static char inkey_aux(void)
     /* Check for possible macro */
     k = macro_find_check(buf);
 
+
     /* No macro pending */
     if (k < 0)
         return (ch);
@@ -2052,6 +2054,7 @@ char inkey(void)
     /* Restore the cursor */
     Term_set_cursor(cursor_state);
 
+
     /* Cancel the various "global parameters" */
     inkey_base = inkey_xtra = inkey_flag = inkey_scan = FALSE;
 
@@ -2069,7 +2072,7 @@ void bell(cptr reason)
 
     if (character_generated && reason)
     {
-        message_add(reason, MSG_BELL);
+        message_add(sq_tr(reason), MSG_BELL); //SQ: site #10 - bell() skips msg_print_aux
 
         /* Window stuff */
         p_ptr->window |= (PW_MESSAGE);
@@ -2784,6 +2787,8 @@ static void msg_print_aux(u16b type, cptr msg)
     byte color;
     int w, h;
 
+    msg = sq_tr(msg); //SQ: site #4 - look the whole message up before it is measured
+
     /* Obtain the size */
     (void)Term_get_size(&w, &h);
 
@@ -2849,6 +2854,8 @@ static void msg_print_aux(u16b type, cptr msg)
                 split = check;
         }
 
+        split = sq_break(t, split); //SQ: site #4 - 2-byte boundary and kinsoku
+
         /* Save the split character */
         oops = t[split];
 
@@ -2865,7 +2872,8 @@ static void msg_print_aux(u16b type, cptr msg)
         t[split] = oops;
 
         /* Insert a space */
-        t[--split] = ' ';
+        if (!sq_cp932_text()) //SQ: this overwrites a byte; that splits a 2-byte char
+            t[--split] = ' '; //SQ: (english only, and only for ascii saves)
 
         /* Prepare to recurse on the rest of "buf" */
         t += split;
@@ -3139,6 +3147,24 @@ void text_out_to_screen(byte a, cptr str)
             continue;
         }
 
+        //SQ: site #5 - place a 2-byte char as one unit; wrap by column, not by space
+        if (sq_cp932_text() && sq_is_sjis_lead((unsigned char)*s) && s[1])
+        {
+            if (sq_wrap_here(s, x, wrap, wid)) //SQ:
+            {
+                x = text_out_indent;
+                y++;
+                Term_erase(x, y, 255);
+            }
+            Term_addch(a, s[0]);
+            Term_addch(a, s[1]);
+            x += 2;
+            if (x > wrap)
+                x = wrap;
+            s++;
+            continue;
+        } //SQ: end of site #5
+
         /* Clean up the char */
         ch = (isprint((unsigned char)*s) ? *s : ' ');
 
@@ -3297,11 +3323,15 @@ void text_out_to_file(byte a, cptr str)
                 len = l_space;
         }
 
+        len = sq_clip(s, len); //SQ: site #5 - never cut a 2-byte char in half
+        if (sq_cp932_text() && (len <= 0)) //SQ: the width cannot hold one char; let it stick out
+            len = 2; //SQ:
+
         /* Write that line to file */
         for (n = 0; n < len; n++)
         {
             /* Ensure the character is printable */
-            ch = (isprint(s[n]) ? s[n] : ' ');
+            ch = (sq_is_text_byte((unsigned char)s[n]) ? s[n] : ' '); //SQ: pass 0x80+ through
 
             /* Write out the character */
             fputc(ch, text_out_file);
@@ -3346,7 +3376,7 @@ void text_out(cptr str) { text_out_c(TERM_WHITE, str); }
  * Output text to the screen (in color) or to a file depending on the
  * selected hook.
  */
-void text_out_c(byte a, cptr str) { text_out_hook(a, str); }
+void text_out_c(byte a, cptr str) { text_out_hook(a, sq_tr(str)); } //SQ: site #16 - text_out() writes byte by byte, so #1/#2 never see the whole fragment
 
 /*
  * Clear part of the screen
@@ -3363,6 +3393,11 @@ void clear_from(int row)
     }
 }
 
+/* SQ: the adapter's text-input flag (site #18). Declared here rather than via
+ * sq_shim.h so that silq/src never sees an adapter header - same practice as
+ * gensoband/src. See docs/SILQ_HD2D_GAPS.md 5.4 (W1). */
+extern void sq_text_input_set(int active); //SQ:
+
 /*
  * Get some input at the cursor location.
  *
@@ -3390,6 +3425,9 @@ bool askfor_aux(char* buf, size_t len)
 
     bool done = FALSE;
 
+    //SQ: W1 - raise it for the whole wait; the ui drops the IME everywhere else
+    sq_text_input_set(1); //SQ:
+
     /* Locate the cursor */
     Term_locate(&x, &y);
 
@@ -3438,14 +3476,51 @@ bool askfor_aux(char* buf, size_t len)
         case 0x7F:
         case '\010':
         {
-            if (k > 0)
-                k--;
+            /*
+             * SQ: site #19 (A1-4) - step back one *character*, not one byte.
+             * Walking a single byte off a two-byte character leaves a lead byte
+             * with nothing behind it, and the next redraw prints garbage.
+             */
+            if (k > 0) //SQ:
+                k -= (size_t)sq_last_char_bytes(buf, (int)k); //SQ:
             break;
         }
 
         default:
         {
-            if ((k < len - 1) && (isprint((unsigned char)ch)))
+            /*
+             * SQ: site #19 (A1-4) - accept CP932 two-byte characters.
+             *
+             * isprint() is locale dependent and rejects 0x80+, so a japanese
+             * character used to fall into bell() - and bell() calls flush(), which
+             * throws the rest of the queue away, so the second byte went too.
+             *
+             * The trailing byte is read right here instead of going round the loop,
+             * for the same reason: anything that bells in between would eat it.
+             *
+             * inkey_base is required.  Without it the byte goes through macro and
+             * keymap processing and comes back as something else - measured on
+             * 2026-08-22, a trailing 0x8E came back as 0xA6, so the two characters
+             * that were typed turned into one wrong one.
+             *
+             * Both bytes must fit, so that the buffer never ends mid-character.
+             */
+            if (sq_is_sjis_lead((unsigned char)ch)) //SQ:
+            { //SQ:
+                char trail; //SQ:
+                inkey_base = TRUE; //SQ:
+                trail = inkey(); //SQ:
+                if (((k + 2) <= (len - 1)) && sq_is_sjis_trail((unsigned char)trail)) //SQ:
+                { //SQ:
+                    buf[k++] = ch; //SQ:
+                    buf[k++] = trail; //SQ:
+                } //SQ:
+                else //SQ:
+                { //SQ:
+                    bell("Illegal edit key!"); //SQ:
+                } //SQ:
+            } //SQ:
+            else if ((k < len - 1) && (isprint((unsigned char)ch)))
             {
                 buf[k++] = ch;
             }
@@ -3465,6 +3540,8 @@ bool askfor_aux(char* buf, size_t len)
         Term_putstr(x, y, -1, TERM_WHITE, buf);
     }
 
+    sq_text_input_set(0); //SQ: W1
+
     /* Done */
     return (ch != ESCAPE);
 }
@@ -3485,6 +3562,20 @@ bool askfor_name(char* buf, size_t len)
     bool done = FALSE;
     bool new_default_name = FALSE;
 
+    /*
+     * SQ: W1 - raise the flag so that the IME shows what is being typed and the
+     * soft keyboard comes up on Android.
+     *
+     * SQ: (A1-4) the name deliberately stays ASCII - the player decided so on
+     * 2026-08-22 (design 12.5, question A-2, option c).  process_player_name() in
+     * files.c turns every byte that is not a letter or a digit into '_' and then
+     * cuts the result to 8 characters on Windows, so every japanese name would
+     * become "________" and two different characters would silently write over
+     * each other's save file.  The isprint() below is what holds it back;
+     * askfor_aux() - inscriptions, notes, searches - does take japanese.
+     */
+    sq_text_input_set(1); //SQ: W1
+
     /* Locate the cursor */
     Term_locate(&x, &y);
 
@@ -3581,6 +3672,8 @@ bool askfor_name(char* buf, size_t len)
         }
     }
 
+    sq_text_input_set(0); //SQ: W1
+
     /* Done */
     return (ch != ESCAPE);
 }
diff --git a/src/xtra1.c b/src/xtra1.c
index 1ae3d6c..39e38d3 100644
--- a/src/xtra1.c
+++ b/src/xtra1.c
@@ -9,6 +9,7 @@
  */
 
 #include "angband.h"
+#include "sq_lang_c.h" //SQ: japanese layer (docs/SILQ_JA_DESIGN.md 4)
 
 /*
  * Determines the total melee damage dice (before criticals and slays)
@@ -519,13 +520,17 @@ static void prt_song(void)
     // show the first song
     if (p_ptr->song1 != SNG_NOTHING)
     {
-        c_put_str(TERM_L_BLUE, song1_name + 8, ROW_SONG, COL_SONG);
+        //SQ: site #12 - "Song of " is a prefix only in english; skipping 8 bytes
+        //SQ: would cut a 2-byte character in half
+        c_put_str(TERM_L_BLUE, song1_name + (sq_lang_enabled() ? 0 : 8), //SQ:
+            ROW_SONG, COL_SONG); //SQ:
     }
 
     // show the second song
     if (p_ptr->song2 != SNG_NOTHING)
     {
-        c_put_str(TERM_BLUE, song2_name + 8, ROW_SONG + 1, COL_SONG);
+        c_put_str(TERM_BLUE, song2_name + (sq_lang_enabled() ? 0 : 8), //SQ: site #12
+            ROW_SONG + 1, COL_SONG); //SQ:
     }
 }
 
@@ -774,7 +779,7 @@ static void prt_state(void)
         int n = p_ptr->resting;
 
         /* Start with "Rest" */
-        my_strcpy(text, "Rest      ", sizeof(text));
+        my_strcpy(text, sq_tr("Rest      "), sizeof(text)); //SQ: site #11 - the digits are patched into text[5..9] afterwards, so the japanese must be 10 bytes too
 
         /* Extensive (timed) rest */
         if (n >= 1000)
@@ -1040,17 +1045,17 @@ bool get_alertness_text(
 
             if (m_ptr->stance == STANCE_FLEEING)
             {
-                my_strcpy(text, "Fleeing", text_size);
+                my_strcpy(text, sq_tr("Fleeing"), text_size); //SQ: site #11 - morale is appended after this
                 *color = TERM_VIOLET;
             }
             else if (m_ptr->stance == STANCE_CONFIDENT)
             {
-                my_strcpy(text, "Confident", text_size);
+                my_strcpy(text, sq_tr("Confident"), text_size); //SQ: site #11
                 *color = TERM_L_WHITE;
             }
             else if (m_ptr->stance == STANCE_AGGRESSIVE)
             {
-                my_strcpy(text, "Aggress", text_size);
+                my_strcpy(text, sq_tr("Aggress"), text_size); //SQ: site #11
                 *color = TERM_L_WHITE;
             }
 
diff --git a/src/z-form.c b/src/z-form.c
index d9f0f77..61a5440 100644
--- a/src/z-form.c
+++ b/src/z-form.c
@@ -11,6 +11,7 @@
 /* Purpose: Low level text formatting -BEN- */
 
 #include "z-form.h"
+#include "sq_lang_c.h" //SQ: japanese layer (docs/SILQ_JA_DESIGN.md 4)
 
 #include "z-util.h"
 #include "z-virt.h"
@@ -207,6 +208,8 @@ size_t vstrnfmt(char* buf, size_t max, cptr fmt, va_list vp)
     if (!fmt)
         fmt = "";
 
+    fmt = sq_tr_fmt(fmt); //SQ: site #3 - translate the format itself, not the result
+
     /* Begin the buffer */
     n = 0;
 
@@ -513,6 +516,8 @@ size_t vstrnfmt(char* buf, size_t max, cptr fmt, va_list vp)
             if (!arg)
                 arg = "";
 
+            arg = sq_tr_arg(arg); //SQ: site #9 - "%s" fragments ("hits you") reach no other hook
+
             /* Prevent buffer overflows */
             (void)my_strcpy(arg2, arg, sizeof(arg2));
 
diff --git a/src/z-term.c b/src/z-term.c
index f3e4577..4046d1a 100644
--- a/src/z-term.c
+++ b/src/z-term.c
@@ -11,6 +11,7 @@
 /* Purpose: a generic, efficient, terminal window package -BEN- */
 
 #include "z-term.h"
+#include "sq_lang_c.h" //SQ: japanese layer (docs/SILQ_JA_DESIGN.md 4)
 
 #include "z-virt.h"
 
@@ -1493,6 +1494,8 @@ errr Term_addstr(int n, byte a, cptr s)
     if (Term->scr->cu)
         return (-1);
 
+    s = sq_tr(s); //SQ: sites #1/#2 - Term_putstr() reaches the screen through here
+
     /* Obtain maximal length */
     k = (n < 0) ? (w + 1) : n;
 
@@ -1504,6 +1507,8 @@ errr Term_addstr(int n, byte a, cptr s)
     if (Term->scr->cx + n >= w)
         res = n = w - Term->scr->cx;
 
+    n = sq_clip(s, n); //SQ: never cut a 2-byte char in half
+
     /* Queue the first "n" characters for display */
     Term_queue_chars(Term->scr->cx, Term->scr->cy, n, a, s);
 
